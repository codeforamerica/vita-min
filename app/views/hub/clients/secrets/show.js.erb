$('#js-secret-<%= id_suffix %>').html(
  "<%= escape_javascript render(partial: @partial_path, **@partial_params) %>"
);
$('#js-toggle-secret-<%= id_suffix %>').html(
  "<%= escape_javascript link_to('Hide', hide_secret_hub_client_path(params.permit(:secret_name, :secret_record_id)), remote: true) %>"
);
