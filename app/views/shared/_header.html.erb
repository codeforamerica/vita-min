<header class="main-header">
  <div class="toolbar">
    <div class="toolbar__left">
      <div class="toolbar__item main-header__title">
        <%= link_to root_path, class: "toolbar__logo-text main-header__logo" do %>
          GetYourRefund.org
        <% end %>
      </div>
    </div>
    <div class="toolbar__right">
      <% if I18n.locale != :en %>
        <%= link_to("English", "?" + request.query_parameters.merge("new_locale" => "en").to_query, class: "toolbar__item text--small", lang: 'en') %>
      <% end %>
      <% if I18n.locale != :es %>
        <%= link_to "Español", "?" + request.query_parameters.merge("new_locale" => "es").to_query, class: "toolbar__item text--small", lang: 'es' %>
      <% end %>
    </div>
  </div>
</header>