<% if flash.any? %>
  <% flash.each do |type, message| %>
    <div class="slab slab--flash flash--<%= type %>">
      <div class="grid">
        <div class="grid__item" role="alert">
          <%= message %>
        </div>
      </div>
    </div>
  <% end %>
<% end %>