<%
  title = t(".title")
  content_for :page_title, title
%>

<% content_for :card do %>
  <%= form_with model: @form, url: { action: :update }, local: true, method: "put", builder: VitaMinFormBuilder do |f| %>
    <h1 class="h2"><%= t(".helper_1_html") %></h1>
    <div class="white-group">
    <%= f.vita_min_money_field(:rent_paid, t('.label_html'), options: { placeholder: '0' }, classes: ["form-width--long"]) %>
    </div>
    <p><%= t(".helper_2") %></p>

    <div class="reveal">
      <button class="reveal__button"><%= t('.anchor_homestead_helper_heading') %></button>
      <div class="reveal__content">
        <p><%= t('.anchor_homestead_helper_description') %></p>
      </div>
    </div>

    <div class="reveal">
      <button class="reveal__button"><%= t('.mobile_homeowners_helper_heading') %></button>
      <div class="reveal__content">
        <p><%= t('.mobile_homeowners_helper_description') %></p>
      </div>
    </div>

    <% if params[:return_to_review].present? %>
      <%= hidden_field_tag "return_to_review", params[:return_to_review] %>
    <% end %>

    <%= f.continue %>
  <% end %>
<% end %>
