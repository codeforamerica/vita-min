<% title = t('.title', state_name: States.name_for_key(params[:us_state].upcase)) %>

<% content_for :page_title, title %>
<% content_for :card do %>
  <%= image_tag("icons/check.svg", alt: "") %>

  <h1 class="h2">
    <%= title %>
  </h1>

  <p>
    <%= t(".email_text_update") %>
  </p>

  <p>
    <%= t(".help_friend_or_family") %>
    <!-- TODO - replace with real link -->
    <%= link_to t(".share_direct_file_link"), "" %>
  </p>

  <p>
    <%= link_to t(".register_to_vote"), "" %>
  </p>

  <p>
    <!-- TODO - replace with real link -->
    <%= link_to t(".apply_to_state_benefits", state_name: States.name_for_key(params[:us_state].upcase)), "" %>
  </p>

  <%= link_to "Download PDF", StateFile::Questions::SubmissionPdfsController.to_path_helper(us_state: params[:us_state], action: :show, id: EfileSubmission.where(data_source: current_intake).first), class: "button button--primary" %>
  <p>
  <%= link_to "Show XML", StateFile::Questions::ConfirmationController.to_path_helper(us_state: params[:us_state], action: :show_xml), class: 'dev-button' %>
    <%= link_to "Explain Calculations", StateFile::Questions::ConfirmationController.to_path_helper(us_state: params[:us_state], action: :explain_calculations), class: 'dev-button' %>
  </p>
<% end %>
