<%
    title = t(".title")
    content_for :page_title, title
%>

<% content_for :card do %>
  <h1 class="h2"><%= title %></h1>

  <%= form_with model: @form, url: { action: :update }, local: true, method: "put", builder: VitaMinFormBuilder do |f| %>
    <div class="white-group">
      <%= f.vita_min_money_field(:property_tax_paid, t('.label_html',filing_year: current_tax_year), options: { placeholder: '0' }, classes: ["form-width--long"]) %>
    </div>

    <div class="reveal">
      <button class="reveal__button"><%= t('.helper_anchor_heading') %></button>
      <div class="reveal__content">
        <p><%= t('.helper_anchor_description_html') %></p>
      </div>
    </div>

    <div class="reveal">
      <button class="reveal__button"><%= t('.helper_heading') %></button>
      <div class="reveal__content">
        <p><%= t('.helper_description') %></p>
      </div>
    </div>

    <%= render "state_file/questions/shared/continue_button", f: f %>
  <% end %>
<% end %>
