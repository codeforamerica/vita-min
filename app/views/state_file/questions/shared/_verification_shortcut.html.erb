<% unless acts_like_production? %>
  <%=
    link_to(
        "I'm on a #{Rails.env} environment, skip this",
        StateFile::Questions::CodeVerifiedController.to_path_helper,
        class: 'dev-button'
    )
  %>
<% end %>