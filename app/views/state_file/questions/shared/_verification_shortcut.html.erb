<% unless acts_like_production? %>
  <%=
    link_to(
        "I'm on a #{Rails.env} environment, skip this",
        StateFile::Questions::CodeVerifiedController.to_path_helper(us_state: current_state_code),
        class: 'dev-button'
    )
  %>
<% end %>