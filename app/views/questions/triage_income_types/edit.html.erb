<% content_for :page_title, t('.title') %>

<% content_for :form_card do %>
  <h1 class="form-question">
    <%= content_for(:page_title) %>
  </h1>

  <p><strong><%= t('.income_types_label') %></strong></p>

  <%= form_with model: @form, url: current_path, local: true, method: "put", builder: VitaMinFormBuilder, html: { class: "form-card" } do |f| %>
    <div class="form-card__stacked-checkboxes spacing-above-0">
      <%= f.cfa_checkbox(:income_type_rent, t('.income_types.rental'), options: { checked_value: "yes", unchecked_value: "no" }) %>
      <%= f.cfa_checkbox(:income_type_farm, t('.income_types.farm'), options: { checked_value: "yes", unchecked_value: "no" }) %>
      <%= f.cfa_checkbox(:none_of_the_above, t("general.none_of_the_above"), options: { id: "none__checkbox", checked_value: "yes", unchecked_value: "no" }) %>
    </div>

    <div class="spacing-above-60">
      <%= f.submit t('general.continue'), class: "button button--primary button--wide text--centered" %>
    </div>
  <% end %>
<% end %>
