<% @main_question = t("views.questions.spouse_email_address.title") %>

<% content_for :page_title, @main_question %>

<% content_for :form_card do %>
  <%= form_with model: @form, url: current_path, local: true, method: "put", builder: VitaMinFormBuilder, html: { class: "form-card form-card--long" } do |f| %>
    <h1 class="h2"><%= @main_question %></h1>
    <p class="text--help">
      <%=t("views.questions.spouse_email_address.info") %>
    </p>

    <div class="form-card__content">
      <%= f.cfa_input_field(:spouse_email_address, t("views.questions.spouse_email_address.spouse_email_address"), classes: ["form-width--long"]) %>
      <%= f.cfa_input_field(:spouse_email_address_confirmation, t("views.questions.spouse_email_address.spouse_email_address_confirmation"), classes: ["form-width--long"]) %>
    </div>

    <button class="button button--primary" type="submit">
      <%=t("general.continue") %>
    </button>
  <% end %>
<% end %>