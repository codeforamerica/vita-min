<% content_for :form_question, t("views.questions.asset_sale_loss.title", :year => current_intake.year_before_most_recent_filing_year, :count => current_intake.filing_joint_yes? ? 2 : 1) %>

<% content_for :form_help_text, t("views.questions.asset_sale_loss.help_text", :year => current_intake.year_before_most_recent_filing_year) %>

<% content_for :method_name, :reported_asset_sale_loss %>