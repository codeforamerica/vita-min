<% content_for :form_question, t("views.questions.asset_sale_loss.title", :year => current_intake.year_before_most_recent_filing_year, :count => current_intake.filer_count) %>

<% content_for :form_help_text, t("views.questions.asset_sale_loss.help_text", :year => current_intake.year_before_most_recent_filing_year) %>