<% content_for :page_title, t("views.stimulus.visit_stimulus_faq.title") %>

<% content_for :form_card do %>
  <div class="form-card">
    <h1 class="form-question">
      <%= content_for(:page_title) %>
    </h1>

    <p>
      <%=t("views.stimulus.visit_stimulus_faq.info") %>
    </p>

    <%= link_to stimulus_path, class: "button button--primary text--centered button--wide spacing-above-60" do %>
      <%=t("views.stimulus.visit_stimulus_faq.cta") %>
    <% end %>
  </div>
<% end %>
