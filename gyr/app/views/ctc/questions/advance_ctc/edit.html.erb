<% @main_question = current_intake.puerto_rico_filing? ? t("views.ctc.questions.advance_ctc.puerto_rico.title") :  t("views.ctc.questions.advance_ctc.title", adv_ctc_estimate: @adv_ctc_estimate) %>

<% content_for :page_title, @main_question %>

<% content_for :card do %>
  <h1 class="h2"><%= @main_question %></h1>

  <% if current_intake.puerto_rico_filing? %>
    <%= render 'puerto_rico_form' %>
  <% else %>
    <%= render 'form' %>
  <% end %>
<% end %>
