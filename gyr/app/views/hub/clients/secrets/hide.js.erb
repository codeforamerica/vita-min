$('#js-secret-<%= id_suffix %>').html(
  "<%= escape_javascript render(partial: @partial_path) %>"
);
$('#js-toggle-secret-<%= id_suffix %>').html(
  "<%= escape_javascript link_to('View', show_secret_hub_client_path(params.permit(:secret_name, :secret_record_id)), remote: true) %>"
);
